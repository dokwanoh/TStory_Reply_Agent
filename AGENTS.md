# 티스토리 확산봇 작업 경계

- 사용자의 기존 게시봇 프로젝트와 공존한다. 기존 프로젝트의 파일, 프로필, 프로세스, 예약 작업을 변경하지 않는다.
- 현재 브라우저 설정과 검증 증거는 Git에서 제외된 로컬 `BROWSER_SETUP.md`를 읽는다. 파일이 없으면 연결 상태를 새로 확인하고, 다른 컴퓨터의 식별자나 경로를 추측하지 않는다.
- 브라우저 작업은 `티스토리 확산봇` 프로필의 확인된 extension 연결만 사용한다. 재연결 시 최신 브라우저 목록에서 프로필명과 extensionInstanceId를 확인한다. 숫자 browser ID는 영구 식별자가 아니다.
- 전용 연결이 없거나 식별이 불일치하면 멈춘다. 일반 Chrome 네이티브 앱, 기본 Chrome 연결, 내장 브라우저로 대체하지 않는다. 전역 키보드·마우스 조작으로 다른 작업의 포커스를 변경하지 않는다.
- 사용자가 지정한 기존 티스토리 계정을 사용한다. 계정 쿠키나 인증 파일을 복사하지 않는다.
- 방문 대상은 홈에 인기글로 표시된 글과 로그인 피드의 이웃 글이다. 사용자는 댓글 자동 게시와 공감·구독을 요청했다.
- 댓글 중복은 게시글 단위로 판단한다. 같은 블로그의 다른 게시글에 내 댓글이 있어도 새 게시글에는 댓글을 작성한다. 블로그당 하루 1회 제한은 적용하지 않으며 전체 일일 한도는 유지한다.
- 봇이 실제 구현되고 동작을 검증하기 전에는 프로필 설정만으로 자동 실행 완료를 주장하지 않는다.
- 확산봇 회차 실행은 `RUNBOOK.md`를 따른다. 외부 행동 전 로컬 CLI로 예약하고, 화면 확인 뒤 결과를 기록한다. CLI만으로 브라우저 자동화가 실행된다고 주장하지 않는다.
- 방문 시 구독 작업 전 화면에서 미구독으로 확인된 블로그에 새 댓글을 작성할 때는 마지막에 사용자 지정 문구 `앞으로 친하게 지네요~!`를 정확히 한 번 붙인다. 이미 구독 중이면 붙이지 않는다. 해당 게시글에 기존 내 댓글이 있으면 이 문구를 남기기 위한 추가 댓글도 작성하지 않는다.

## Git 이력과 원격 동기화

- 이 프로젝트의 원격 저장소는 `https://github.com/dokwanoh/TStory_Reply_Agent.git`이다.
- 사용자는 이후 모든 코드 변경 이력을 원격에 계속 최신화하도록 승인했다. 각 의미 있는 변경 단위를 검증한 뒤 관련 코드·테스트·문서를 함께 커밋하고 현재 작업 브랜치에 푸시한다. Git 작업에는 git-master 스킬을 적용한다.
- 작업 시작 시 브랜치, 원격, 미커밋 변경과 원격 진행 상태를 확인한다. 기존 이력과 다른 작업자의 변경을 보존한다. 강제 푸시나 이력 재작성은 별도 지시 없이 하지 않는다.
- 작업 종료 전 원격 브랜치 SHA와 로컬 HEAD가 같은지 확인하고 미커밋 변경을 보고한다. 인증·네트워크·보호 브랜치 문제로 푸시가 실패하면 실패와 미동기화 커밋을 명시한다.
- 공개 저장소에 인증 정보, 브라우저 프로필, 쿠키, 비밀번호, 로컬 연결 식별자, 개인 경로, 실행 중 수집한 개인 데이터를 커밋하지 않는다. 무차별 `git add .` 대신 변경 파일을 지정해 스테이징한다.
- 이 규칙은 이 프로젝트 작업 중의 동기화에 적용된다. 별도의 상주 감시 프로세스나 예약 동기화가 설치됐다고 주장하지 않는다.
